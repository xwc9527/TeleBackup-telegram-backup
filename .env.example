# Copy to ".env" and fill in your real token (never commit ".env").
# PyPI token from https://pypi.org/manage/account/token/ — full value usually starts with "pypi-".
TWINE_USERNAME=__token__
TWINE_PASSWORD=pypi-REPLACE_ME
